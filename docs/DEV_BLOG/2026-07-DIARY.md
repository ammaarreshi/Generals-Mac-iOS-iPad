# July 2026

## 02/07/2026
### Documentation & Instruction Updates
- **Audio Backend**: Updated `AGENTS.md` and `.github/instructions` to reinstate OpenAL as the default audio backend, keeping MiniAudio as WIP. Audio fixes must now prioritize OpenAL and be backported to MiniAudio.
- **Base Game Stability**: Updated documentation and rules to reflect that the base game (Generals) is stable. Fixes must be backported from Zero Hour to Generals.
- **Development Diary Layout**: Retained `docs/DEV_BLOG` as the canonical monthly diary location required by `AGENTS.md`; active working material remains under `docs/WORKDIR`.
## 02/07/2026
### Audio Fix: Missing Voiceovers
Fixed a significant issue where in-game voiceovers (like "You are victorious" or General Challenge taunts) were not playing, especially during late-game or heavy combat scenarios.
The issue stemmed from two places:
1. **OpenAL and MiniAudio priority leaks:** When the maximum sample limits were hit (e.g., 64 2D sounds), the game would correctly attempt to cull lower priority sounds. However, `OpenALAudioManager` and `MiniAudioManager` were missing the logic to set the `handleToKill` inside `isPlayingLowerPriority()`. This resulted in the engines continuously generating new hardware sources without freeing older ones, eventually exhausting all physical sources (usually 256) and silently dropping new sounds.
2. **MiniAudio incorrect limits:** `MiniAudioManager::getNumAvailable2DSamples` was calculating available 2D samples against the size of the *entire* `m_playingSounds` list (which in MiniAudio includes 3D sounds and Streams/Music). This effectively kept the available samples at 0 at all times, making it always fall back to the broken priority handling.

Both issues have been resolved by adding proper counting filters in MiniAudio and uncommenting `event->setHandleToKill` in both audio managers' priority checks.

## 10/07/2026
### Sync TheSuperHackers Upstream
- Merged upstream `thesuperhackers/main` into GeneralsX.
- Resolved conflicts in `Core/Libraries/Include/Lib/BaseType.h` (preserved deterministic `WWMath::SqrtOrigin`, integrated `lengthSqr()`).
- Kept `canUpdateGameLogic` signatures intact and resolved logic conflicts in GameEngine.cpp.
- Preserved bone positioning logic in `SlavedUpdate.cpp` and fixed pointer dereferencing for `Coord3D` operations (`*obj->getPosition()`).
- Accepted the upstream re-organization of GameClient UI code and removed deprecated ControlBar files.
- Imported the deterministic GameMath integration but kept it disabled by default until retail replay A/B validation is complete.
- Successfully built the macOS target (`macos-vulkan`).

## 10/07/2026
### Controlled GeneralsX Beta 13 Integration
- Integrated the exact `GeneralsX-Beta-13` tag (`d24b2aad40a485edfcde85a1de8b8aa60db49503`) as a local merge commit on `codex/sync-generalsx-beta13` without pushing or replacing the working runtime.
- Preserved this fork's Apple changes, instruction hierarchy, `docs/DEV_BLOG` workflow, current reference-repository paths, and OpenAL-first architecture. MiniAudio remains an experimental opt-in preset.
- Restored retail-compatible CRC, pathfinding, networking, AI-group, circle-fill, and scripted-camera defaults. Deterministic GameMath is explicitly disabled in primary Linux, macOS, and iOS presets pending replay-corpus validation.
- Fixed audit regressions in signed network slot mapping, Apple `Byte` compatibility, retail ceil/floor conversion, GameMath option gating, `SagePatch.ini` migration safety, and radar view-box reconstruction retries.
- Completed a read-only pre-PR Fable review of the merge against its first parent. It found no P0/P1 regressions; all three P2 and five P3 findings were triaged as pre-PR fixes.
- Restored the retail `WWMath::Acos`/`Asin` arc-trig fallback and limited replay CI to each platform's recorded corpus. Linux Flatpak maps to Linux replays, stale replay files are removed, and empty replay runs now fail.
- Pinned the experimental MiniAudio dependency to the reviewed `0.11.25` release with a SHA-256 integrity check instead of downloading a moving branch.
- Isolated Apple's unsigned MiniAudio `Byte` declaration behind a GameEngineDevice compatibility header so the engine keeps its retail signed `Byte`; the optional `macos-miniaudio` preset then configured and linked both games in a 2,138-step ARM64 build.
- Preserved retail command-ID seeding, rejected point-to-point and broadcast-less interfaces during LAN broadcast discovery, guarded the platform D3DX downsamplers against format conversion, and added an uncapped startup-fade yield without double-throttling capped rendering.
- Removed the merged inactive `LAN86` `fprintf` blocks and their orphaned trace scaffolding while retaining active `DEBUG_LOG` diagnostics.
- Corrected the `GeneralsReplays` gitlink to `d00070157d1e8cd911927eca7499aed29dda0118`, the platform-specific replay corpus referenced by the replay workflow; unrelated upstream changes had moved the pointer backward twice.
- Hardened replay CI so pull requests cannot restore or create plaintext retail-asset caches. Trusted runs cache only the encrypted archive keyed by its verified Git LFS SHA-256 and decrypt it in ephemeral runner storage.
- Made macOS deployment copy `libopenal.1.dylib`, removing the runtime's dependency on the build-tree OpenAL path.
- Recovered only the quarantined trusted build inputs required for validation: the Microsoft `vcpkg` bootstrap/executable and DXVK's pinned `libdisplay-info` generator.
- Completed a clean 1,459-target Zero Hour build and a 787-target base Generals build with AppleClang on macOS ARM64.
- Staged the candidate at `~/GeneralsX/GeneralsZH-Beta13`; verified all graphics/audio libraries loaded from that directory, reached the shell render loop, and exited cleanly. The existing `~/GeneralsX/GeneralsZH` rollback binary remained byte-for-byte unchanged.
- iOS was preserved during conflict resolution but was not built or runtime-tested in this pass.
